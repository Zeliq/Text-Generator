import React from 'react';
import ReactDOM from 'react-dom';
import RandomTextGenerator from './RandomTextGenerator';
import './index.css'; // You can create this CSS file to style your app

ReactDOM.render(
  <React.StrictMode>
    <RandomTextGenerator />
  </React.StrictMode>,
  document.getElementById('root')
);
